const first = 'Abid';
const second = 'Nabid';

const fullName = first + ' ' + second;
console.log(fullName); // Output: Abid Nabid
const fullName2 = first.concat(' ').concat(last);
console.log(fullName2); // Output: Abid Nabid